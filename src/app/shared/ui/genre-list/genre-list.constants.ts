import { AlignDirectionEnum } from "../../../core/utils/enum/align-direction.enum";
import { IList } from "../../common/list/list.interface";

export const DEFAULT_GENRE_LIST: IList = {
    items: [],
    direction: AlignDirectionEnum.VERTICAL
}